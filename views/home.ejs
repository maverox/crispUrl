<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        :host,:root, body{
            margin: 0px;
            /* overflow-y: scroll;/ */
            /* scroll-behavior: smooth; */
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100vh;
        }
        .container {
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            background-color: #253444;
            min-height: 90vh;
        }
        .form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .form-label {
            margin-bottom: 10px;
            height: fit-content;
            font-size: large;
            font-weight: 900;
            color: #ffdada;
        }
        .form-input {
            width: 300px;
            padding: 10px;
            margin-bottom: 10px;
            height: auto;
            border-radius: 5px;
            border: none;
            box-shadow: 5px 2px 10px #253444;
            font-size: large;
        }
        .form-button {
            padding: 10px 20px;
            background-color: #007bff;
            height: auto;
            color: #fff;
            border: none;
            font-size: large;
            border-radius: 5px;
            cursor: pointer;
        }
        .form-input:focus-visible {
            outline: #007bff solid 3px;
        }
        a{
            color: #ffdada;
            text-decoration: none;
            transition: color 0.7s ease-in-out;
        }
        /* a:visited {
            color: #ff3c00
        } */
        a:hover {
            color: #007bff;
        }
    </style>
</head>
<body>
    <!-- <%- include('./partials/header') %> -->
    <div class="container">
        <form action="http://<%= process.env.HOST %>:<%= process.env.PORT %>/url" method="POST" class="form">
            <label for="urlInput" class="form-label">Enter URL:</label>
            <div class="form-input-container">
                <input type="text" id="urlInput" name="url" placeholder="https://www.example.com" required class="form-input">
                <button type="submit" class="form-button">Generate Crisp Link</button>
            </div>
        </form>
        <% if (locals.shortId) { %>
            <div style="font-size: medium; color: lightblue;">
                <% if (locals.shortId) { %>
                   <h2>Here's your crisp Link:  <a href="http://<%= process.env.HOST%>:<%= process.env.PORT %>/<%= locals.shortId %>"><%= locals.shortId %></a></h2>
                <% } %>
                
            </div>
        <% } %>
    </div>
    <%- include('./partials/footer') %>
</body>
</html>
